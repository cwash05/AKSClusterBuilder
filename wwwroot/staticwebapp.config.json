{
    "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": ["/images/*", "/css/*", "/js/*", "/authentication/*"]
    }
    // No additional route rules needed for a basic setup
}
